#include<rf24hub.h>
/*
  MyNetwork::MyNetwork( RF24& _radio ): radio(_radio), frame_size(MAX_FRAME_SIZE)
  {
  }
  void MyNetwork::begin(uint8_t _channel )
  {
     radio.setChannel(_channel);

  }
  bool MyNetwork::available(void)
   {
   }
  uint16_t MyNetwork::peek(MyNetworkHeader& header)
  {
  }
  uint16_t MyNetwork::read(MyNetworkHeader& header, void* message, uint16_t maxlen)
  {
  }
  bool MyNetwork::write(MyNetworkHeader& header,const void* message, uint16_t len)
  {
  }
*/

